/* CRIAR BANCO DE DADOS*/
CREATE DATABASE BD_DML

USE BD_DML

/*CRIAR TABELA E ALTERAR A ESTRUTURA DA MESMA*/
CREATE TABLE TB_DML( 
MATRICULA TINYINT PRIMARY KEY, 
NOME VARCHAR(100) , 
SEXO CHAR(1))

ALTER TABLE TB_DML 
ADD DTNASC DATETIME, CPF CHAR( 11)

ALTER TABLE TB_DML 
ALTER COLUMN DTNASC DATE

ALTER TABLE TB_DML 
DROP COLUMN CPF;


/*INSERINDO REGISTROS EXPLICITANDO AS COLUNAS*/

INSERT INTO TB_DML (MATRICULA, NOME, DTNASC, SEXO) 
VALUES (1, 'PESSOA_A', '2000-12-10','M')

/*INSERINDO VÁRIOS REGISTROS COM APENAS 1 INSERT*/
INSERT INTO TB_DML (MATRICULA, NOME, DTNASC, SEXO) 
VALUES (2, 'PESSOA_B', '2000-12-10','M'), 
	(3,'PESSOA_C','2000-01-01','F')

/*
INSERINDO REGISTROS NÃO EXPLICITANDO AS COLUNAS
OBS: AO EXECUTAR VAI APRESENTAR ERRO, DEPOIS MUDAR A ORDEM
DO VALOR 'M' PELA '2002-01-10'
*/

INSERT TB_DML
VALUES (4, 'PESSOA_D', '2002-01-10', 'M')


/*ATUALIZANDO OS DADOS DE UMA COLUNA DO REGISTRO*/
UPDATE TB_DML
SET SEXO = 'F'
WHERE NOME = 'PESSOA_B'
and MATRICULA = 2


/*ATUALIZANDO OS DADOS DE MAIS DE UMA COLUNA DO REGISTRO*/
UPDATE TB_DML
SET SEXO = 'F', DTNASC = '1997-07-01'
WHERE NOME = 'PESSOA_D'

/*DELETANDO O REGISTRO DA TABELA*/
DELETE FROM TB_DML
WHERE NOME = 'PESSOA_C'
AND MATRICULA = 3